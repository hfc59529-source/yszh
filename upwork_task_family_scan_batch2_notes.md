Status: INCOMPLETE CLEAN BATCH — BOUNDARY HELD

Goal:

- Validate whether `Desk Research + Comparative Analysis + Decision Support` is a stable Upwork task family.

Requested target:

- 20-30 clean records.

Collected in this pass:

- 12 clean included records.

Why not 20 yet:

- The search results returned many jobs that matched keywords but violated the exclusion rules.
- Excluded categories included SEO execution, Amazon-specific work, pure sales/BD, automation engineering, interview/survey-heavy research, and local-identity or tool-heavy roles.
- I did not relax the boundary to hit the count.

Current status:

- Evidence is directionally useful but not enough to upgrade to `PASS — Productization Candidate`.
- Continue collecting only if the same inclusion/exclusion rules can be maintained.

Next action:

- Add 8-18 more clean job-detail records only when they match the task family:
  `public/client inputs -> compare -> synthesize -> recommend/rank/gap/brief`

Required extra fields were added:

- Primary Input Type
- Decision Output
- Buyer Risk if Wrong
- Evidence Depth
